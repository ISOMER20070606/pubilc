# AI_USAGE.md

> 🤖 AI 使用说明：
> 本文件记录了本人在焦糖工作室招新过程中使用 AI Agent（Cline）的完整情况。
> 使用 AI 的部分已在文中标注，最终成果均经本人通过 git diff 检查并验证。

## 任务 1：我的 Agent 使用记录

### 我使用了什么 Agent
- **Cline**：一个 VS Code 里的 AI Coding Agent，能读文件、改代码、执行终端命令、搜索项目、运行程序并根据结果继续工作。

### 安装和配置过程
1. 在 VS Code 扩展市场搜索并安装 Cline 插件；
2. 配置可用的模型 API；
3. 打开项目文件夹后，在对话框里描述需求，Cline 会自动调用工具（读文件、编辑、执行命令）来完成任务。

### 我交给了它什么任务
本次招新期间，我用 Cline 完成了以下工作：
1. **医学 VQA 复现项目**：搭建 LoRA 微调 + RLVR 强化学习流程，修 bug、写训练脚本、生成报告图表；
2. **Git 入门题**：建仓库、写 README 和学习笔记、commit、push；
3. **Linux & Shell 挑战**：完成 9 道题（文件探索、日志统计、管道、重定向、脚本编写、进程管理等）。

### Agent 做了哪些修改
- 编写了 `train_lora.py`、`train_rlvr.py`、`eval` 脚本等代码；
- 修改了 `scripts/analyze.sh`、`scripts/batch-copy.sh`（修复带空格文件名的 bug）；
- 生成了 `output/` 下各题的答案文件；
- 撰写了 `README.md`、`note.md`、`answers/02.md`、`answers/08.md` 等文档。

### git diff 中我看到了什么
通过 `git diff`，我能清楚看到 Agent 的每次改动，例如：
- `batch-copy.sh` 中 `$@` → `"$@"`、`$file` → `"$file"`（修复空格 bug）；
- `analyze.sh` 从空壳 TODO 变成了完整的日志分析脚本；
- `output/` 下新增了各题的结果文件。

每次提交前我都会看一眼 diff，确认改动符合预期。

### 最终结果是否符合预期
符合预期。Linux & Shell 挑战 9 题全部通过 `./check.sh` 验证（8/8 + 附加题），医学 VQA 项目也跑通并得到 SFT 39.41% → RLVR 40.59% 的结果。

## 任务 2：关于 Agent 的理解

### 1. Agent 为什么能读文件、改代码，而普通聊天 AI 不能？
因为 Agent 被提供了**工具（Tool）**。普通聊天 AI 只有「对话」这一个能力，只能输出文字建议；而 Agent 额外拥有「读取文件」「修改文件」「搜索内容」「执行命令」等工具，模型判断何时调用工具、再根据工具返回的结果继续工作。所以 Agent 是「大语言模型 + 工具 + 任务流程」。

### 2. Tool 在 Agent 中起到什么作用？
Tool 是 Agent 的「手和眼睛」，让模型能够**实际操作环境**：读取项目文件、写入修改、搜索代码、执行终端命令、访问外部服务。模型本身不会开文件、也不会敲终端，全靠 Tool 来完成这些动作，并把结果反馈给模型继续决策。

### 3. 为什么项目需要给 Agent 配置一份类似「员工手册」的规则（如 CLAUDE.md）？
因为 Agent 第一次接触项目时，并不知道项目是干什么的、文件该放哪、有什么规范、改完要不要测试、哪些不能动。就像新员工入职需要说明书。通过项目级规则，可以告诉它项目用途、文件组织、修改后要检查、不要乱猜、不要把密码/Token/API Key 写进仓库等，避免它踩坑或造成破坏。

### 4. Agent 为什么可能「忘记」之前说过的内容？额度怎么算？
因为大模型一次能处理的上下文（Context）是有限的。对话历史、代码、工具返回结果、项目规则都会占用上下文，一旦超出限制，前面的内容就可能「顾不上了」。额度通常按 **token** 计算，输入（读入的代码、历史）和输出（生成的内容）都要计入，所以长对话或大文件会更快耗尽额度。

### 5. 如果 Agent 可以随便执行任何终端命令，会有什么风险？
风险很大：可能误删文件（如 `rm -rf`）、修改或泄露敏感信息、执行恶意或不可逆命令、搞坏系统环境。所以应该限制权限、关键操作前人工确认、执行前先看 `pwd`/`ls` 确认位置、不把密码/Token 交给它，并对最终结果负责。

## 任务 3（可选）：MCP / Skills / Tools

（后续如果尝试了 MCP、Skills 等扩展能力，会在这里补充记录。）
